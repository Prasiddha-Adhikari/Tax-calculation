import { Routes } from '@angular/router';
import { VehicleTaxComponent } from './vehicle-tax/vehicle-tax.component';

export const routes: Routes = [
  { path: '', component: VehicleTaxComponent },
];
